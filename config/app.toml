[app]
name = "file-ingest-service"
log_level = "INFO"
env = "DEV"

[service]
run_seconds = 5
poll_interval_seconds = 1.0
data_dir = "data"
allowed_suffixes = [".csv", ".dat", ".txt"]
min_size_bytes = 1
